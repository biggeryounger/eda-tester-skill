# TCL 脚本规范

状态：`DRAFT_NOT_CONFIGURED`

此文件是 Layer 2B 的唯一业务规则入口。规范补充后，每条规则应使用稳定 ID，例如 `TCL-STYLE-001`，并至少包含：

- 规则名称和说明。
- 适用工具和脚本类型。
- 严重级别。
- 可被程序判断的通过条件。
- 正例和反例。

已有 `TEST_ACTION`、`EXPECT`、用例追溯及禁止未完成占位符等检查属于框架临时契约。待正式规范确认后，应为它们分配规则 ID或删除。

